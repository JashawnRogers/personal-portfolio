import { Button, Colors } from '@blueprintjs/core'

const StyledButton = () => {
  return (
    <Button style={{background: Colors.GREEN3}} className="rounded-full text-black" ></Button>
  )
}

export default StyledButton